<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>list</title>
        <style>
            body{
                padding: 20px 50px;
            }
            h1{
                text-align: center;
            }
            table{
                margin: 0 auto;
            }
        </style>
    </head>
    <body>
        <h1>What should I do !?</h1>
        <table border="bold">
            <tr>
                <th>id</th>
                <th>title</th>
                <th>content</th>
                <th>created_at</th>
            </tr>
            <% for (row of list) { %>
            <tr>
                <td><%= row.id %></td>
                <td><%= row.title %></td>
                <td><%= row.content %></td>
                <% const created_at = String(row.created_at).split(' GMT').shift() %>
                <td><%= created_at %></td>
            </tr>
            <% } %>
        </table>
    </body>
</html>